import 'package:flutter/widgets.dart';

class CreateHelperTypeStep extends StatelessWidget {
  const CreateHelperTypeStep({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Type'),
    );
  }
}